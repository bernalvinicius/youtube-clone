import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  informationsVideo: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'initial',
  },
  description: {
    width: '700px',
    marginLeft: '20px',
  },
});
